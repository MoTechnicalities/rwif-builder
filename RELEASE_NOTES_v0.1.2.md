# rwif-builder v0.1.2

Documentation coherence follow-up after `v0.1.1`.

## What Changed

- defines RWIF explicitly as Resonant Wave Information Format in the public docs
- fixes the DCT equation markup in `docs/RWIF_DEEP_DIVE.md` for GitHub-compatible rendering
- keeps the builder pipeline, artifact format, and runtime surface unchanged from `v0.1.1`

## Scope

This is a documentation-only release that aligns the latest tagged snapshot with the current public docs on `main`.