# rwif-builder v0.1.1

Release-coherence follow-up after the initial public launch.

## What Changed

- restores the MIT `LICENSE` file on `main`
- fixes the live companion-repo link in `README.md`
- keeps the runtime surface and shipped builder pipeline unchanged from `v0.1.0`

## Scope

This is a documentation and packaging coherence release, not a feature expansion.